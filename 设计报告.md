#  系统设计报告

## 1. 系统概述

Code2025 是一个针对程序员的专业成长平台，集成了心理测评、社区交流、资源分享等功能，旨在帮助开发者更好地规划职业发展，应对行业挑战，提升个人竞争力。

### 1.1 系统目标

- 为程序员提供心理健康支持和专业成长指导
- 建立良好的社区交流环境，促进知识分享和经验交流
- 通过个性化的测评和建议，帮助程序员制定职业发展计划
- 整合优质的学习资源，促进程序员持续学习和提升

### 1.2 主要功能

- 心理测评：提供多种类型的心理测评，帮助程序员了解自己的心理状态
- 社区交流：支持用户发布帖子、评论互动，分享经验和观点
- 资源分享：整合学习资源、职业规划工具和方法
- AI辅助：基于DeepSeek AI提供智能咨询和建议
- 测评结果融合：基于用户的心理测评结果，提供针对性的咨询服务

## 2. 系统架构设计

### 2.1 总体架构

Code2025采用前后端分离的架构设计：

- 前端：Vue.js框架 + Element Plus组件库
- 后端：Spring Boot框架
- 数据库：MySQL
- AI服务：DeepSeek-R1-DistiII-Qwen-1.5B

### 2.2 技术栈

#### 前端技术栈
- Vue.js 3.x：前端框架
- Vue Router：路由管理
- Axios：HTTP客户端
- Element Plus：UI组件库

#### 后端技术栈
- Spring Boot：后端框架
- MyBatis：ORM框架
- MySQL：关系型数据库
- JWT：用户身份验证

#### 辅助工具
- Maven：项目管理工具
- Lombok：简化Java开发
- Hutool：Java工具类库

## 3. 数据库设计

### 3.1 数据库ER图

系统使用MySQL数据库，主要包含以下实体和关系：

- 用户(User)
- 管理员(Admin)
- 分类(Category)
- 通知(Notice)
- 简介(Introduction)
- 测试结果(TestResult)
- 社区帖子(CommunityPost)
- 帖子评论(PostComment)



### 3.2 数据库表结构

#### 用户表(user)
```sql
CREATE TABLE IF NOT EXISTS `user` (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    name VARCHAR(50),
    phone VARCHAR(20),
    email VARCHAR(100),
    role VARCHAR(20),
    avatar VARCHAR(255),
    state INT DEFAULT 1
);
```

#### 管理员表(admin)
```sql
CREATE TABLE IF NOT EXISTS `admin` (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    name VARCHAR(50),
    phone VARCHAR(20),
    email VARCHAR(100),
    role VARCHAR(20),
    avatar VARCHAR(255)
);
```

#### 分类表(category)
```sql
CREATE TABLE IF NOT EXISTS `category` (
    id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) NOT NULL
);
```

#### 通知表(notice)
```sql
CREATE TABLE IF NOT EXISTS `notice` (
    id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(100) NOT NULL,
    content TEXT,
    time DATETIME
);
```

#### 简介表(introduction)
```sql
CREATE TABLE IF NOT EXISTS `introduction` (
    id INT PRIMARY KEY AUTO_INCREMENT,
    img VARCHAR(255),
    title VARCHAR(100) NOT NULL,
    content TEXT,
    time DATETIME,
    category_id INT,
    user_id INT,
    FOREIGN KEY (category_id) REFERENCES category(id),
    FOREIGN KEY (user_id) REFERENCES user(id)
);
```

#### 测试结果表(test_result)
```sql
CREATE TABLE IF NOT EXISTS `test_result` (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    test_type VARCHAR(50) NOT NULL,
    score INT,
    result_level VARCHAR(50),
    result_description TEXT,
    result_advice TEXT,
    test_date DATETIME,
    answers TEXT,
    FOREIGN KEY (user_id) REFERENCES user(id)
);
```

#### 社区帖子表(community_post)
```sql
CREATE TABLE IF NOT EXISTS community_post (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    title VARCHAR(100) NOT NULL,
    content TEXT NOT NULL,
    category VARCHAR(20) NOT NULL,
    likes INT DEFAULT 0,
    comments INT DEFAULT 0,
    is_anonymous BOOLEAN DEFAULT FALSE,
    create_time DATETIME NOT NULL,
    update_time DATETIME NOT NULL,
    status VARCHAR(10) DEFAULT 'normal',
    FOREIGN KEY (user_id) REFERENCES user(id)
);
```

#### 帖子评论表(post_comment)
```sql
CREATE TABLE IF NOT EXISTS post_comment (
    id INT PRIMARY KEY AUTO_INCREMENT,
    post_id INT NOT NULL,
    user_id INT NOT NULL,
    content TEXT NOT NULL,
    parent_id INT,
    is_anonymous BOOLEAN DEFAULT FALSE,
    create_time DATETIME NOT NULL,
    status VARCHAR(10) DEFAULT 'normal',
    FOREIGN KEY (post_id) REFERENCES community_post(id),
    FOREIGN KEY (user_id) REFERENCES user(id),
    FOREIGN KEY (parent_id) REFERENCES post_comment(id)
);
```

## 4. 模块设计

### 4.1 用户管理模块

#### 功能描述
- 用户注册与登录
- 个人信息管理
- 权限控制

#### 核心接口
- `/api/user/login`：用户登录
- `/api/user/register`：用户注册
- `/api/user/info`：获取用户信息
- `/api/user/update`：更新用户信息

### 4.2 社区交流模块

#### 功能描述
- 发布帖子
- 评论互动
- 匿名发布
- 点赞功能

#### 核心接口
- `/api/community/posts`：获取帖子列表
- `/api/community/posts/{id}`：获取帖子详情
- `/api/community/comments`：获取评论列表
- `/api/community/posts/like/{id}`：点赞功能

### 4.3 心理测评模块

#### 功能描述
- 多种类型的心理测评
- 测评结果分析
- 结果存储与查询

#### 核心接口
- `/api/test/list`：获取测评列表
- `/api/test/submit`：提交测评答案
- `/api/test/results`：获取测评结果
- `/api/test/advice`：获取基于测评的建议

### 4.4 AI辅助咨询模块

#### 功能描述
- 基于DeepSeek AI的智能咨询
- 测评结果融合的针对性建议
- 职业发展指导

#### 核心接口
- `/api/ai/chat`：AI对话接口
- `/api/ai/analysis`：AI分析接口
- `/api/ai/career-advice`：职业建议接口

## 5. 安全设计

### 5.1 身份验证

系统采用JWT(JSON Web Token)进行身份验证，主要流程：
1. 用户登录成功后，服务器生成JWT
2. 客户端存储JWT，并在后续请求中携带
3. 服务器验证JWT的有效性，提供相应的权限

### 5.2 数据安全

- 密码加密：使用加密算法存储用户密码
- 数据校验：对用户输入进行验证，防止SQL注入和XSS攻击
- 敏感数据保护：对敏感信息进行脱敏处理

## 6. 部署方案

### 6.1 环境要求

- JDK 17+
- MySQL 8.0+
- Node.js 16+
- 内存：至少4GB
- 存储：至少20GB

### 6.2 部署步骤

1. 数据库部署
   - 创建数据库：`code2025`
   - 执行SQL脚本：`full_schema.sql`

2. 后端部署
   - 编译打包：`mvn clean package`
   - 运行JAR包：`java -jar springboot-0.0.1-SNAPSHOT.jar`

3. 前端部署
   - 安装依赖：`npm install`
   - 构建项目：`npm run build`
   - 部署到Web服务器

## 7. 系统限制与未来展望

### 7.1 当前限制

- AI模型的理解能力和回答质量有待提高
- 缺乏大规模并发处理的优化
- 社区内容监管机制不够完善

### 7.2 未来展望

- 引入更多类型的心理测评和职业能力测评
- 增强AI模型的理解能力和专业性
- 完善社区内容监管机制
- 增加实时通讯功能，促进用户之间的直接交流
- 扩展移动端支持，提供更便捷的使用体验

## 8. 总结

Code2025系统旨在为程序员提供全方位的心理支持和职业发展帮助。通过集成心理测评、社区交流、AI辅助等功能，为程序员打造一个专业的成长平台。系统采用前后端分离架构，具有良好的可扩展性和维护性。

未来，我们将持续优化系统功能，提升用户体验，努力成为程序员职业成长的得力助手。 
